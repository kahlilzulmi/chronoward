"$schema" = "schemas/schema.json"

[default]
description = "Allow Chronoward to check Android tracking permissions, listen for context events, schedule exact pre-alert alarms, and control the ongoing timer notification"
permissions = [
    "allow-check-permissions",
    "allow-request-permissions",
    "allow-register-listener",
    "allow-schedule-exact-alarm",
    "allow-cancel-exact-alarm",
    "allow-start-ongoing-notification",
    "allow-update-notification-state",
    "allow-clear-ongoing-notification",
]
